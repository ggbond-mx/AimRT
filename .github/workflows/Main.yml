name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    # 指定官方 runner
    runs-on: ubuntu-latest    # 可选择的官方 runner

    steps:
      - uses: actions/checkout@v3
      - name: Run a test
        run: echo "Hello from GitHub-hosted runner !"